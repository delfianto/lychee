#!/usr/bin/env bash
# SessionStart: anchor the assistant to lychee's stack, conventions, and decisions.
printf '%s\n' '{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"lychee is a self-hosted manga/comic/ebook media server (scan-and-index). Monorepo: backend/ (Python 3.14 / FastAPI / SQLAlchemy 2.0 / Alembic / SQLite; layered router->service->repository; QA gate from backend/: uv run ruff format . && uv run ruff check --fix . && uv run basedpyright && uv run pytest) and frontend/ (Vue 3 / TypeScript / Vite / Tailwind CSS v4 / DaisyUI 5 / Bun; typecheck from frontend/: bun run typecheck). STYLING: use DaisyUI component classes and canonical Tailwind utilities (shrink-0, grow) — NOT raw arbitrary values or deprecated aliases. Architecture is recorded as ADRs in notes/decisions/ (01-18); reference research in notes/. Run everything via the root justfile (just --list). Work on the current branch; never git push unless the user explicitly asks."}}'
